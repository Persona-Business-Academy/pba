import PricingPage from '@/components/templates/PricingPage';

export const metadata = {
  title: 'PBA Pricing Plans',
};

const Pricing = () => {
  return <PricingPage />;
};

export default Pricing;
