import React, { FC } from 'react';

type QueryParamContextProps = {
  children: React.ReactNode;
};

const QueryParamContext: FC<QueryParamContextProps> = () => {
  return <div>QueryParamContext</div>;
};

export default QueryParamContext;
